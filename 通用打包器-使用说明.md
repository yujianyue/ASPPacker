# ASP 单文件 EXE 通用打包器（Windows）

> **v1.1 更新（重要）**：
> 1. **修复“打包成功后程序闪退”**：旧版写入 zip 中央目录时容量少算了文件名总长，源码包文件较多时堆溢出，在输出文件写完、释放内存瞬间崩溃（表现为“生成成功后闪退”）。已修复并经 AddressSanitizer 对照验证（旧公式必溢出、新公式干净），同时兼容标准 zip 工具校验。
> 2. **修复生成的程序全站 404**：旧版内嵌配置中的相对根目录（wwwroot）未转为绝对路径，生成的 EXE 首次运行（无磁盘 ini 时）所有页面 404。已修复。
> 3. **崩溃自诊断**：打包器与生成的程序崩溃时不再静默退出，会在 EXE 同级生成 `<程序名>.crash.log`（异常代码 + 地址），可直接反馈定位。
> 4. **数据种子多级目录**：首次运行释放数据文件时自动逐级创建子目录（如 `wwwroot\data\wage.mdb`）。
> 5. **落盘规则可自定义**：新增「动态数据目录」（默认 `/data/|/upload/`）与「缓存后缀」（默认 `.json|.ini`）两项定义——所有文件都会打包进 exe，但**只有命中这两项的才落盘**供程序读写，其余仅存于 exe（磁盘更干净、数据不外露）。
> 6. 配套文档：**《ASP打包要点.md》**（ASP 程序压缩包制作要点、内置对象/方法支持情况、自检清单、常见问题）。

## 一、这是什么
把一个 **ASP 程序 zip 包** 与内置的 **aspfox 迷你 ASP 服务器** 打包成一个独立的 Windows exe 文件。双击即可在本机 / 内网 / 公网 IP + 端口访问，无需 IIS、无需安装、代码不可见（在 exe 内）。

## 二、使用方法
1. 把 `Packer.exe` 放到 Windows 上，**双击运行**（无需安装任何环境，单文件）。
   - 启动后：**输出目录**默认填为 `Packer.exe` 所在目录；若该程序目录下**恰好只有一个** `.zip` / `.ico`，会自动填入「源码」与「图标」；文件选择对话框也默认从该程序目录开始。即路径按「程序所在目录 + 相对目录」组合，便于随程序一起分发。
2. 在界面填写参数（见下图与第三节），其中 **ASP 程序 zip 文件** 为必选：
   - 用任意工具（7-Zip / `zip` 命令 / Windows 压缩等）把你的 ASP 站点打包成 zip，包含 `index.asp` 等页面与 `assets/` 等静态资源即可。
   - **图标 ico** 可选，留空使用默认图标。
   - 若把整个站点文件夹整体压缩（压缩包里多一层目录，如 `工资查询系统/index.asp`），打包器会**自动剥离这层顶层目录**，确保 `index.asp` 落在根目录，无需手动调整；若压缩包内有多个并列顶层目录或根级文件，则不剥离（保持原样）。
3. 点「开始打包」，下方日志区显示进度；完成后给出输出 exe 的完整路径。
4. 点「关于」按钮可查看软件名称、版本、简介、免责声明、注意事项与问题反馈方式。

> UI 已针对中文做了 Unicode 化处理（资源以 UTF-16 存储），并采用紧凑布局；在任意 Windows 语言环境（含中文 GBK）下均不会乱码。

## 二之一、关于本软件（「关于」对话框内容）
| 项目 | 内容 |
|---|---|
| 软件名称 | ASP 单文件 EXE 通用打包器 |
| 版本 | v1.1 |
| 简介 | 将 ASP 程序与内置迷你 ASP 服务器打包为单个 EXE，双击即用，支持本机 / 内网 / 公网访问，代码内嵌不可见。 |
| 免责声明 | 本工具仅用于合法授权范围内的部署与学习。使用者须遵守所在地区法律法规；因使用本工具产生的一切后果由使用者自行承担，作者不承担任何责任。请勿将其用于任何未授权或违法用途。 |
| 注意事项 | 1) 输出 EXE 首次运行会在同级目录释放数据目录（即“运行目录名”）；2) 防火墙 / 安全软件可能拦截局域网访问，需放行对应端口；3) 仅 32 位 EXE，Windows 7 及以上可用；4) 图标留空则使用内置默认图标。 |
| 问题反馈 | yujianyue  15058593138@qq.com |

## 三、参数详解
| 界面参数 | 含义 | 写入位置 | 默认 |
|---|---|---|---|
| ASP 程序 zip 文件 | 站点源码，打包时自动解压、剥顶层目录、store 重打包后**自附加到 exe 尾部** | exe 尾部自附加段 (app.zip) | — |
| 图标 ico（可选） | 输出 exe 的图标 | 资源 101 (图标组) | 默认图标 |
| 软件标题 | 窗口/托盘显示名 | 自附加配置 (app.ini `Title`) | 我的ASP应用 |
| 不可访问的文件夹 | 命中返回 403，多值用 `\|` 分隔 | app.ini `DenyDirs` | /data |
| 不可访问的文件后缀 | 命中返回 403，多值用 `\|` 分隔 | app.ini `DenyExts` | .mdb |
| 仅本机可访问的路径 | 仅回环地址(127.x)可访问，其余 IP 403，多值用 `\|` 分隔 | app.ini `LocalOnly` | install.asp\|repass.asp |
| 动态数据目录 | 命中该路径段的文件**落盘供读写**，多值用 `\|` 分隔，填 `-` 表示无 | app.ini `DataDirs` | /data/\|/upload/ |
| 缓存后缀 | 命中该扩展名的文件**落盘供读写**，多值用 `\|` 分隔，填 `-` 表示无 | app.ini `CacheExts` | .json\|.ini |
| 运行目录名（压缩到 exe 的路径） | 数据释放到 exe 同级的该目录 | app.ini `Root` | wwwroot |
| 输出目录 | 生成 exe 的目录，留空则用 zip 所在目录 | — | zip 所在目录 |

> 多层优先级：**编译默认 < 打包器烧录(app.ini) < 运行时磁盘 aspfox.ini**（运行时可再覆盖）。

## 四、输出 exe 的行为
- **程序代码与配置以“自附加”方式存放在 exe 文件尾部**（末尾 20 字节为识别标记），不依赖 Windows 资源注入，在任何环境（含安全软件拦截资源修改）下都可靠生效。
- **体积自检**：打包完成日志会显示 `输出大小：N 字节（壳 X + 程序数据 Y + 配置 Z + 标记 20）`。输出应 ≈ 壳 + 你的程序大小；若远小于此，说明源码 zip 选小了（例如误选了示例小包）。
- **防呆**：若误选打包器自带的占位示例包，会直接报错拒绝打包，不会生成“占位程序”。
- HTTP 监听 `0.0.0.0`（所有网卡），端口依次尝试 `80 → 8080 → 8000 → 8888`。
- **zip 里所有文件都打包进 exe**；运行时分两类：
  - **纯内存（永不落盘）**：`.asp` / `.asa` / `assets/*` / `manual.html`，以及**未命中落盘规则的一切文件**（图片、css、js、htm 等）——不可见、防拷贝、请求时从内存直接输出；
  - **内存 + 落盘（可读写）**：命中「动态数据目录」（如 `data/`、`upload/`）或「缓存后缀」（如 `.json`、`.ini`）的文件，首次运行释放到 `<运行目录名>\`（多级子目录自动创建，已存在不覆盖），之后**磁盘版优先于内存版**——程序或管理员改磁盘文件即生效。
- ⚠️ **Access 数据库（.mdb/.accdb）所在的目录必须列入「动态数据目录」**，否则数据库不落盘，Jet 打不开会报错（默认 `/data/` 已覆盖常见情形；若库在 `db/` 等其他目录请自行添加）。
- 右键托盘图标：关于 / 设置（端口、根目录、拒绝规则）/ 重启 / 退出 / **开放局域网访问（防火墙）**。
- 公网访问需在路由器/防火墙做端口转发；若装了 360 / 火绒 / 电脑管家等安全软件，其自带防火墙会独立拦截，需在该软件中手动放行本程序联网。
- `.mdb` 与以 `data` 开头的文件默认不可直接下载（403），但磁盘上仍可见、程序可读写。

## 五、已知限制
- 打包器与生成的 exe 均须在 **Windows** 运行；当前构建环境无 Windows，**未做实机运行验证**，请在目标 Windows 上确认双击与局域网访问。
- zip 内文件名建议使用 ASCII / UTF-8；GUI 选择路径使用 Unicode，极端旧的 ANSI 路径可能异常。
- 内嵌 app.zip 为 store 模式（不压缩、代码不可见但不加密）。
- 若某 ASP 程序内二进制资源恰好包含 `PK\x03\x04` 签名，服务器扫描可能误判（极罕见，标准 ASP 站点不受影响）。
- `.asp/.asa/assets/*` 永不落盘（固定规则）；其余文件是否落盘由「动态数据目录」与「缓存后缀」决定。
